import { Text } from "@mantine/core";
import classes from './TitleName.module.css';

export default function TitleName() {
    return (
        <Text classNames={{ root: classes.root }}>AMIN YARITS FIRDAUS</Text>
    )
}